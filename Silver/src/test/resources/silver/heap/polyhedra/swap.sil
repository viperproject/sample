field val: Int

method swap(x: Ref, y: Ref)
{
    var oldx: Ref
    oldx := x
    var oldy: Ref
    oldy := y
    var t: Int
    t := x.val
    x.val := y.val
    y.val := t
    assert (oldx.val == y.val && oldy.val == x.val)
}
