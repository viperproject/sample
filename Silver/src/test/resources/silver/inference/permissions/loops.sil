
field f: Int

method test1(x: Ref)
  //requires acc(x.f, write)
{
    var b: Bool
    b := true

    while (b)
      //invariant acc(x.f, write) 
    {
        x.f := 4
        b := false
    }
}

method test2(x: Ref)
  //requires acc(x.f, read)
{
    while (x.f < 3)
      //invariant acc(x.f, read)
    {

    }
}
