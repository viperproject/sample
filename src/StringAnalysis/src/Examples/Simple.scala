package Examples

class Simple {
		def CreateString(x : Int) : Unit = {
				var s = "0";
				//var t = Console.readLine();
				//var z = Console.readLine();
				//var w = t+z+"a";
				//t = "";
				//if(x>0)
				//	s = "ba";
				//var z = t+s;
				
				//var i : Int = x;
				while(x > 0) {
					//i = i - 1;
					s = s + "0";
				}
				/*
				var s = "aef"; //"it's sunny";
				var t = "b"; //"it's cloudy";
				var u = "c";
				var v = "doqw";
				var w = s.concat(u);
				var y = t.concat(w);
				var z = "";
				if(s.contains("a")) {
					z = w;
				} else {
					z = y;
				}
				if(s.contains("b")) {
					z = z;
				} else {
					z = v;
				}
				z = z.substring(1,3);
				*/
				//var z = t.substring(2,13);
				//s += "g";
				//if(t.indexOf('a') >= 0)
				//	s = "itpippog"; 
				//if(t.indexOf('a')<0)
				//	s = "topolino";
		}
}