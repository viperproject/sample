method foo(this: Ref)
{
    var i: Int := 0
    i := bar(this, i)
    i := i
}

method bar(this: Ref, i: Int) returns (k: Int)
{
    k := i + 1
}

method test(this: Ref) {
  var i: Int := 2
  i := bar(this, i)
  i := i
}