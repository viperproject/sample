method a(x: Int, c: Bool) returns (r: Int)
{
    if (c)
    {
        r := 0
    }
    else
    {
        r := b(x, c)
        r := r + 1
    }
}

method b(x: Int, c: Bool) returns (r: Int)
{
    if (c)
    {
        r := 0
    }
    else
    {
        r := a(x, c)
        r := r - 1
    }
}