var val: Int

method get(c: Ref) returns (i: Int) {
    i := c.val
}

method testGet() {
    var i: Int
    var c: Ref
    c := new()
    c.val := 42
    i := get(c)
    assert (i == 42)
    assert (c.val == 42)
}