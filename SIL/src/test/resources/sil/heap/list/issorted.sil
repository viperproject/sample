var val: Int
var next: Ref

method isSorted(this: Ref) returns (res: Bool) {
    var cur: Ref
    cur := this
    res := true
    while (res && cur != null && cur.next != null) {
        if (cur.val > cur.next.val) {
            res := false
        }
        cur := cur.next
    }
}